.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Claude Code local settings (per-developer, not for commit)
.claude/

# Private working docs (video script, submission pitch) — kept local for
# production + submission, not committed to public repo
/private/
